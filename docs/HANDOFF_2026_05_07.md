# Session Handoff — 2026-05-07 (Post-CG Hardening)

## Summary of Work
This session successfully closed the **OS Font Parity** gap and executed a massive hardening sprint for **Canada Goose (Kasada)**. While the final pass is still blocked by a 304 response on `/mfc`, we have eliminated 5 major detection vectors.

### 1. Worker Parity (The "TextEncoder" fix) — DONE
- **Gap**: Kasada runs its PoW solver in a Web Worker. Our workers lacked `TextEncoder`, `Crypto`, and `URL`, causing the solver to crash with `TypeError`.
- **Fix**: Created `shared_apis_bootstrap.js` containing all spec-mandated shared APIs.
- **Result**: Workers now have full parity with the main window. Kasada PoW now runs to completion in our worker environment.

### 2. WebAuthn & FedCM Prototypes — DONE
- **Fix**: Implemented the full `Credential` hierarchy (`PublicKeyCredential`, `IdentityCredential`, `FederatedCredential`, `PasswordCredential`).
- **Parity**: Fixed prototype inheritance (e.g., `PublicKeyCredential instanceof Credential === true`) and added `getClientCapabilities()` stubs.
- **Result**: Headless detection probes targeting the absence of WebAuthn now pass.

### 3. H2 & TLS Extension SOTA — DONE
- **H2 Settings**: Updated `h2_client.rs` to match Chrome 147's exact SETTINGS: `MAX_CONCURRENT_STREAMS=1000`, `ENABLE_CONNECT_PROTOCOL`, and `NO_RFC7540_PRIORITIES`.
- **TLS Permutation**: Implemented manual TLS extension ordering via FFI (`SSL_CTX_set_extension_permutation`). We now send the 17 core extensions in the exact bucketed order of headed Chrome 147.
- **ALPS Payload**: Expanded the ALPS payload to 66 bytes, including the full 8-setting frame and an `ACCEPT_CH` frame.

### 4. Audio & Timing Calibration — DONE
- **Makeup Gain**: Implemented a threshold-dependent makeup gain curve in `audio.rs`. We now match Chrome's audio sum at both -24dB and -50dB probes within 0.4% accuracy.
- **Kasada Realism**: Corrected the `rst` (Relative Start Time) and `d` (Delta) formulas in PoW solutions to use realistic page-relative values instead of Unix timestamps.

### 5. MFC Force-Refresh — DONE
- **Fix**: Added `now_unix_ms` cache-busting to the `/mfc` endpoint and explicitly filtered out `if-none-match` headers in `HttpClient` to force a `200 OK`.

## Current Status: Canada Goose
- **Solver**: **RUNNING**. The PoW completes and POSTs successfully.
- **Blocker**: The `/mfc` request returns **304 Not Modified**, assumedly because Kasada's edge is still flagging our TLS fingerprint or IP as "low trust" and refusing to issue the `x-kpsdk-fc` token.
- **H2 Status**: Consistently negotiating **HTTP/1.1** instead of **H2** on Canada Goose (though H2 works on other sites). This is our primary remaining "tell".

## Next Strategic Task: Pass Canada Goose & Passing 100+ Sites
1. **H2 Negotiation Recovery**: Deep dive into why Canada Goose specifically rejects H2. Suspect **BoringSSL Extension Order** vs. **Chrome Shuffling Buckets** (Chrome shuffles *within* buckets, we are currently fixed).
2. **Global Surface Polish**: Close the `window.ownPropertyNames.length` gap (currently 369 in oxide vs 980 in Chrome). 600+ missing properties are a high-confidence bot signal.
3. **Wildberries Reload Fix**: Plumb `Accept-CH` upgrade tracking through `Page` navigation loop.

## Action Plan
1. Implement **TLS Extension Shuffling Buckets** in `tls.rs`.
2. Bulk-register missing **HTML/SVG interface objects** to reach 900+ window property parity.
3. Verify **Wildberries Accept-CH** propagation.
