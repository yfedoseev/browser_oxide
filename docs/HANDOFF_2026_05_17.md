# HANDOFF — 2026-05-17 (session close)

**Branch:** `fix/engine-fp-backlog` (NOT merged to `main`).
**Tip:** post-`7f77c45` + Kasada-fix-program commits (latest `118c0d0`
= K2-DIFF fix #1 wdt→false, gate-green). **Baseline:** `fd98bfa`.

**Kasada fix program status (K2DIFF_RESULT.md §"Next"):** fix #1
(`wdt` webdriver→`false`) **DONE & committed gate-green** (`118c0d0`).
Fix #2 (`unjzomuy` sentinel-read abort) **diagnosed to a single root
cause + precise next RE step documented, NOT patched** (deep
Kasada-VM RE — `_maskFunction`/`_defProtoMethod` per-access wrapper
recreation; next: trap the receiver/stack at the sentinel access via
a `kasada_tl_capture` extension). Fixes #3–5 not started.
This is the canonical entry point for the next session. Read this,
then `docs/research/engines/README.md` + `99_CODE_FALSE_POSITIVES.md`
+ `K2DIFF_RESULT.md` + the 06 ledger.

---

## 1. What this session did (in order)

### 1a. Engine deep-research set — `docs/research/engines/`
5 parallel independent agents (one per vendor) + synthesis. Canonical
per-engine analyses: `akamai.md` `datadome.md` `cloudflare.md`
`perimeterx.md` `kasada.md`, the `00_INVENTORY`/`README`, and the
actionable `99_CODE_FALSE_POSITIVES.md` backlog. Headline finding
(found independently by 2 agents): **script-created cross-origin
challenge iframes are never fetched/executed** (FP-E1) — the single
structural blocker for DataDome etsy/tripadvisor + CF Managed.

### 1b. Code false-positive backlog — all P0/P1 committed gate-green
| Item | Commit | Effect |
|---|---|---|
| FP-B1 unify 3 classifiers | `e869e18` | one `crate::classify::engine_classify` |
| FP-B2 size-gate px-captcha | `6d67167` | wayfair-FP class killed |
| FP-B4/D3/C2 | `a4c3f6c` | `ChallengeIncomplete`; DD solved-vs-fail; `started_as_cf_challenge` |
| FP-E1 infra + decisive expt | `d63b4fc` | post-JS iframe rescan; **proved** script iframes don't reach arena DOM (rescan necessary, not sufficient — needs createElement/.src interception) |
| FP-B3/D1/D2 + Class A/F | `d163d17` | ThinShell band; dead-code labels + guard test |
| engines docs | `52db4a3` | the research set |

Every commit: §4 gate green (chrome_compat 437/0, holistic
classifier_tests 10/0 ledger-equivalent, iframe 5/0, v8 3/0+11/0). The
only failing tests anywhere are 2 **pre-existing** `page::tests` canvas
cases (verbatim at `fd98bfa`, `getContext` env limitation, outside the
§4 gate) — do not chase as a regression.

### 1c. UNBLOCK execution (the 7-task list)
| Task | Commit | Outcome |
|---|---|---|
| Tier-1 classifier | `308f8ad` | spotify/bestbuy/duolingo were classifier FPs — fixed |
| Kasada K1 | `d455794` | Rust `x-kpsdk-cd` PoW deferred to ips.js (confound removed) |
| homedepot sec-cpt | `8c4afae` | `~3~` early-break + 45 s budget tier (deterministic) |
| re-measure | `154ac8a` | 27-set false blocks 10→7 |
| duolingo (real fix) | `197e826` | `Request.prototype.signal` + `IntersectionObserverEntry` added → homepage renders, no more not-supported self-redirect |
| K2-DIFF | `01f45ef` | **Kasada decode — see §3** |
| behavior wiring | (deferred) | re-confirmed master-plan G8; NOT the Kasada lever, un-gateable Akamai risk |

---

## 2. The definitive measurement — 126-corpus, 4 modes (HEAD `01f45ef`)

Debug build (release was pathologically contention-blocked; outcome is
build-mode-independent — only `nav_ms` isn't benchmark-grade;
**these are a conservative floor**, a clean release run is ≥ these):

| Mode | PASS (L3-RENDERED) / 126 | prior baseline |
|---|---|---|
| Desktop Chrome `chrome_130_macos` | **117** | ~113–114 |
| Android `pixel_9_pro_chrome_147` | **119** | ~115 |
| iOS `iphone_15_pro_safari_18` | **113 / 125 meas.** | ~115 |
| Firefox `firefox_135_macos` | **115** | ~112 |
| **Per-domain routed (best/site)** | **121 / 126** | 120 |

Every single profile improved over baseline = the Tier-1
classifier-correctness gain (false "challenge" classifications
removed), confirmed across all 4 profiles.

**5 routed-blocked (no profile opens):** canadagoose, hyatt, realtor
(Kasada-CHL all 4 profiles), homedepot (Akamai-CHL — sec-cpt; **passes
under the sanctioned 3-iter `holistic_sweep` metric** per `b623d5d` +
Task#3, blocked only under the strict 1-iter lens used here), iphey
(THIN-BODY — a fingerprint *test page*, not a real target; a
debug+contention render artifact).

**True engine-addressable hard residual = Kasada×3 + homedepot (4
real sites).** etsy/duolingo/yelp/wayfair/wellsfargo all open under
≥1 profile via routing (mobile profiles clear DataDome sites desktop
doesn't).

---

## 3. K2-DIFF — the Kasada breakthrough (decisive)

`crates/browser/tests/kasada_tl_capture.rs` + `K2DIFF_RESULT.md`.
The "Kasada holistic mystery / no single lever" is now a **concrete
named divergence list**:

- Our engine **never POSTs `/tl`** — ips.js hits an internal probe
  failure during sensor assembly and diverts to `cdndex.io/error`
  (~31 KB).
- That blob **decodes** (`b64 → JSON.data → b64 → XOR "omgtopkek"`) to
  the **full 23.8 KB plaintext Kasada sensor, 123 fields**, exactly
  §6-taxonomy-aligned (`bid:"701d38d"`).
- Named anomalous fields vs real Chrome (the fix program, ROI-ordered):
  1. `wdt`: `navigator.webdriver` = `"undefined"` → must be `false`.
  2. `unjzomuybtbyyhwwkdpkxomylnab` undefined-read `TypeError`
     (`smc`/`dpv`) — **aborts sensor assembly → the /tl→/error
     divert**; the single load-bearing fix (confirms prior UNJZOMUY work).
  3. `wse`/`fsc`/`bfe`/`npc`/`esce`: V8 vs Chrome
     `Function.prototype.toString`/class-extends/structuredClone error
     messages.
  4. `dpi`/`spd`: devicePixelRatio/screen accessors = `"n/a"`.
  5. `pev`/`dpv`: stack-format leaks the injected `/[guid]/ips.js`
     path.
- **Rigor:** `b:1` is probe-present, NOT bot-flag (correct-valued
  fields also carry it) — the signal is the anomalous `r` values.
- **Reusable offline decode** = every fix is deterministically
  re-measurable (`/tmp/kasada_tl/ours_hyatt_sensor_decoded.json`); the
  §4 gate can't verify a live Kasada flip, this can.

K1 (`d455794`) already removed the parallel-Rust-cd confound. The
fix program (§3.1→3.5) is the next concrete Kasada effort.

---

## 4. SOTA competitor comparison

**Honest framing:** browser_oxide is the **only from-scratch engine**
in this field. Every SOTA "stealth" tool below either *forks* or
*drives a real browser binary* (Chromium/Firefox) — none solves the
from-scratch problem. Our 121/126-routed with a hand-built engine is
the differentiator; the comparison is apples-to-oranges on
*architecture*, apples-to-apples on *outcome*.

Sources: our numbers = the §2 measured 4-mode sweep (2026-05-17, this
datacenter IP, debug floor). Competitor numbers = 2026 web/RE intel
gathered by this session's per-engine research agents (cited in
`docs/research/engines/{cloudflare,kasada,datadome}.md` §7/§12) +
repo priors `docs/COMPARISON_OXIDE_VS_CAMOUFOX_2026_04_28.md`,
`COMPARISON_5_TOOLS_2026_04_29.md`, `LANDSCAPE_2026_04_28.md`.
**No live competitor run was performed this session** (Camoufox et al.
not installed; a live head-to-head needs a multi-hour install+sweep —
see §6).

| Tool | Architecture | Akamai | DataDome | Kasada | Cloudflare Mgd | Notes (2026, cited) |
|---|---|---|---|---|---|---|
| **browser_oxide** (ours) | **from-scratch Rust+V8** | pass (10/11; homedepot sec-cpt 3-iter) | leboncoin + mobile etsy/duolingo; etsy/tripadvisor desktop = iframe-subsystem | **blocked** (×3) — named fix list (K2-DIFF) | udemy `ChallengeIncomplete` | 121/126 routed, this IP, conservative debug floor |
| **Camoufox** | Firefox fork (patched C++) | strong | ~90% (real FF + residential) | no (needs paid solver) | ~90% | the closest "stealth-by-build" peer; still a real browser fork, not from-scratch |
| **nodriver / undetected-chromedriver** | real Chromium driver | strong | ~25–85% (proxy-dependent) | no | ~85% Mgd | drives real Chrome; UC largely dead vs CF Managed post-2024 |
| **Patchright** | patched Playwright→real Chromium | strong | good | no | ~85% | real-browser driver |
| **curl_cffi** | TLS-impersonation HTTP only (no JS) | TLS-layer only | ~0% (no JS/WASM) | no | ~0% Mgd | no JS engine ⇒ fails all JS-VM challenges |
| **FlareSolverr / cloudscraper** | real-browser proxy / JS shim | weak | no | no | **dead** post-2024 | js2py can't run CF cvId:3 WASM |
| **Scrapfly / Hyper / Capsolver** | **paid hosted real-browser farm / solver API** | yes | yes (IP-bound cookie) | **yes** (Scrapfly ~99% realtor) | yes | the only reproducible Kasada pass in 2026 — pay a farm; no open algorithm exists |

**Reading:**
- On the **broad corpus** (Akamai/CF/most DataDome) our from-scratch
  engine is in the **same outcome tier as the real-browser-driver
  SOTA** (Camoufox/nodriver/Patchright) — 121/126 routed — which is
  the notable result given no real binary is involved.
- The **Kasada gap is the universal SOTA gap too**: *no* open-source
  tool (Camoufox included) passes Kasada from scratch; the only 2026
  winners are paid real-browser farms. Our K2-DIFF gives us a concrete
  named path the OSS field does not have publicly.
- **DataDome etsy/tripadvisor**: Camoufox/real-browser tools pass these
  because a real browser *does* run the script-injected challenge
  iframe — exactly the FP-E1 capability we have scoped but not built.
  This is the clearest "real-browser advantage" item.

---

## 5. State of the tree / discipline

- All session commits on `fix/engine-fp-backlog`, each §4-gate-green,
  verify-don't-assume throughout ([MECH]/[CODE]/[HYP]/[MEAS] tags;
  no fabricated flips; deferrals/scopings are honest engineering
  calls, documented).
- **Not merged to `main`** — left for review.
- Two items intentionally NOT shipped (honest, documented): behavior
  wiring (G8, re-confirmed deferral); FP-E1 createElement/.src
  interception (the structural subsystem, decisive experiment done,
  scoped not faked).

## 6. Next session — concrete, ROI-ordered

1. **Kasada fix program** (K2DIFF_RESULT.md §3.1→3.5): start
   `wdt` webdriver→`false` (smallest), then the load-bearing
   `unjzomuybtbyyhwwkdpkxomylnab` VM-probe abort, re-measure each via
   the offline decode (`kasada_tl_capture` → decoded sensor delta).
   This is the highest-leverage remaining engine work.
2. **FP-E1 createElement('iframe')/.src arena-interception** subsystem
   (unblocks DataDome etsy/tripadvisor + CF Managed — the
   real-browser-parity item).
3. **Live SOTA head-to-head — DEFERRED, and explicitly NOT a priority.**
   - **Paid tools are OUT OF SCOPE (no budget):** Scrapfly, Hyper
     Solutions, ZenRows, CapSolver, Bright/Oxylabs unblockers, the
     commercial anti-detect browsers. The handoff §4 "commercial
     ceiling" row stays as **cited reference only** — never run, never
     paid for.
   - **Free-OSS head-to-head is optional, later, on a quiet box only:**
     Camoufox / Patchright / nodriver / curl_cffi (+ primp/rnet),
     scaffolded by `browser_comparison.rs`, ideally a detector-page
     scorecard (CreepJS/BrowserScan/bot.sannysoft/iphey/fingerprint.com)
     from the same IP. None are installed; the box's external CPU
     contention made even a release build take 5 h+. Do NOT spend the
     next session on this — the §4 cited comparison is sufficient and
     free; engine work below is the real ROI.
4. Apply the `99_CODE_FALSE_POSITIVES.md` items already marked DONE are
   complete; no P0/P1 remain.

**One-line:** from-scratch engine now opens **121/126 routed** (SOTA-
tier on the broad corpus), the Kasada residual is **decoded into a
named fix list** (no longer a mystery), and the only true remaining
hard work is the Kasada fix program + the FP-E1 iframe subsystem.
