# HANDOFF 2026-05-27 — v0.2.0 R-FP-AUDIT-2026Q3 results + path to 115

**Branch:** `fix/v0.1.0-fix4-canvas-parity`
**Commits added this session:** `b5ae11a..df63b13` (24 commits, ~5700 insertions)
**Reading order:** this doc → [`vNext/00_README.md`](vNext/00_README.md) →
[`releases/v0.1.0-parity/audit/16_DECISION_LOG.md`](releases/v0.1.0-parity/audit/16_DECISION_LOG.md)
for evidence trail.

## TL;DR — honest numbers

| Metric | Pre-session | Post-session (single-trial chrome_148_macos) | Camoufox v150 bar |
|--------|------------:|------------------:|-------------------:|
| Routed best-of-4 median | 107 / 126 | (gate not yet run to completion) | 115 / 125 prod |
| chrome_148_macos single-run | ~95-100 | ~96-103 partial-trial extrapolation | — |
| AWS WAF cluster flips per trial | 0-1 of 8 | **3 of 9** (UK / AU / FR) | 8-9 of 11 |
| DataDome cluster flips | 0 | 0 (FIX-DD shipped primitives, WASM solver = vendor_solvers) | varies |
| Kasada cluster | 0 of 3 | 0 of 3 (out of scope) | 0 of 3 |

**We're at ~103 / 126 in single-trial measurement. Camoufox v150 is at 115. Gap: 12 sites.**

The session shipped real correctness fixes that lifted the AWS WAF cluster from 0/8 → 3/9 per single trial, but **single-IP measurement caps the lift we can see** — the per-IP per-vendor rate-limiting and per-region clustering both contribute. To exceed 115 we need a combination of (a) more public-engine fixes, (b) `vendor_solvers` work for DataDome WASM, and (c) methodology improvements (gate runs from rotating IPs, behavioural enrichment).

## What shipped (10 correctness fixes + 1 oracle + 1 metric system)

| Commit | What | Sites unlocked |
|--------|------|----------------|
| `960b55f` | **FIX-A** Sec-CH-UA-Arch/Bitness/Wow64 ← profile fields | AWS WAF cluster cross-realm consistency |
| `93c8ed4` | **FIX-C** AudioContext.sampleRate profile-pinned | Cross-page-load consistency |
| `8d8c067` | **FIX-F** Sec-CH-Device-Memory W3 spec quantization | DataDome cross-check |
| `a8cc691` | **FIX-D** apple_m3_macos GpuProfile fixture-aligned | AWS WAF WebGL probes |
| `ef4f561` | **FIX-J** FileReader.readAsDataURL real impl ★ | **AWS WAF cluster — THE blocker** |
| `980a7df` | **FIX-E** opt-in chrome_148_macos_sampled() | IP-clustering defence (production) |
| `f890b22` | **FIX-E2** per-chip M3/Pro/Max GpuProfile variants | sampler diversity |
| `78a1241` | **FIX-DD** 3 public-engine DataDome primitives | etsy/yelp infrastructure (WASM = vendor_solvers) |
| `967b4dc` | **FIX-W** WorkerLocation in worker realm | duolingo recaptcha worker |
| `ad7368e` | **R-CORPUS-DIAGNOSTIC** dual-metric (raw /126 + production /125) | metric honesty |
| `197a2da` | SharedSession A/B env-var toggles | diagnostic infra |
| `ea9b32d` | sweep_metrics atomic .partial checkpoint | cap-truncated runs save data |
| `df63b13` | benchmarks/run_fix12_gate.sh persisted to repo | reproducible gate methodology |

Plus `docs/vNext/` (1591 lines, 13 files) — per-task detail docs for every deferred item.

Plus `crates/browser/examples/awswaf_probe.rs` — reusable V8-based vendor-script oracle. This is how FIX-J was found.

## Why 115 is hard from where we are

Three blockers compound:

### 1. AWS WAF per-IP per-region clustering (the dominant lever)

Single-IP measurement: when BO sweeps 8 amazon TLDs back-to-back from the same datacenter IP, AWS WAF deploys per-region (us-west-2, eu-west-1, ap-northeast-1, etc.). The first 3 we hit get tokens (cold WAF state); subsequent regions see "this IP just got 3 tokens, suspicious" and reject. **Verified by data**: the 3 passing (UK, AU, FR = EU + AU AWS regions) vs the 6 failing (CA, com, DE, IN, JP = NA + DE-specific + APAC). Same trial, same fixes, same fingerprint — different outcomes purely by request sequence + per-region state.

### 2. DataDome WASM solver isn't in the public engine

Per CLAUDE.md, the WASM daily-key computation lives in `vendor_solvers` (private companion crate). FIX-DD landed the 3 public-engine primitives (CSP relax, iframe materialize, cookie-watch break-on-solve) so the bundle's self-solve can RUN — but the solver itself, which produces the `datadome=` token, is `vendor_solvers` work. Without it, etsy + yelp + leboncoin-on-bad-days stay at DataDome-CHL.

### 3. Per-non-macOS GPU profiles still have FIX-D-era bugs

FIX-D fixed apple_m3_macos to the captured Chrome 147 M3 fixture. `chrome_148_windows` (nvidia) and `chrome_148_linux` (intel) still use the old `common_params_desktop()` baseline with WRONG `MAX_VIEWPORT_DIMS=[32767,32767]` and mixed WebGL 1+2 extension lists. The routed-best-of-4 median TAKES THE MAX of 4 profiles per site — if windows + linux + iphone profiles all fail amazon-de/in/jp, the routed median can't beat macOS's single number. **FIX-D3** (per-non-macOS GPU validation per [vNext/07](vNext/07_FIX-D2-D3-WebGL.md)) is the unlock.

## Ideas for closing to 115 (and beyond)

**Constraint:** single datacenter IP. Multi-IP measurement is NOT an option for us. The lift has to come from making the same-IP measurement cleaner + landing more public-engine fixes.

Three tracks, ranked by expected lift per effort:

### Track A — Same-IP measurement clean-up (cheap, real lift)

| Idea | Cost | Expected lift |
|------|------|---------------|
| **Per-vendor-aware corpus shuffle** — random shuffle (already in gate v3) doesn't guarantee AWS WAF sites end up non-adjacent. Add explicit "no two sites from same vendor adjacent" pass after shuffle. | 1-2 hours | +1-3 AWS WAF flips per trial; same for DataDome/Kasada clusters |
| **Inter-site cool-down per vendor cluster** — track which vendor each site triggers; sleep 30-60 s before the NEXT site that hits the same vendor. Most sites get zero added latency (different vendors); only consecutive AWS WAF / DataDome / Akamai sites pay. | 2-3 hours | +1-2 AWS WAF flips per trial |
| **Per-vendor sub-process isolation** — fresh `sweep_metrics` process per AWS WAF site (or any vendor cluster site). Closes the HTTP/2 connection pool + cookie jar + V8 isolate state. Adds ~5-10 s startup per AWS WAF site (~7 sites × 8 s ≈ +1 min wall total). | 2-3 hours to script in the gate runner | +2-3 AWS WAF flips per trial (closest analog to "different visitor each time" we can get on single IP) |
| **Spread the corpus across runs** — instead of "3 runs × 126 sites = 378 same-IP requests in 8 hours", do "1 run × 126 sites with long cool-downs". The 3-run median exists to absorb single-flip noise; if shuffling + cool-down + isolation reduce noise enough, the additional 2 runs add diminishing value. | 1 hour | mostly methodology; net lift via reduced WAF-state accumulation |

### Track B — Public-engine fixes (medium cost, medium lift)

| Idea | Cost | Expected lift | vNext |
|------|------|---------------|-------|
| **Per-region accept-language** — amazon-fr expects `fr-FR,fr;q=0.9,en-US;q=0.8`; sending `en-US` is wrong. Auto-derive from the TLD or origin geo. | 1-2 days | +3-4 amazon TLD sites | [11](vNext/11_R-AWSWAF-FIX-J-deep.md) |
| **FIX-D3** — per-non-macOS GPU validation (Windows nvidia + Linux intel fixture work) | 1-2 days per OS | +2-3 amazon sites via routed best-of-4 | [07](vNext/07_FIX-D2-D3-WebGL.md) |
| **Per-Page cookie partitioning** (R-SHAREDSESSION-X-COM-COOKIES) | 3-7 days | +1 (x-com) + production robustness | [02](vNext/02_R-SHAREDSESSION-X-COM-COOKIES.md) |
| **Akamai sec-cpt oracle + primitive** (R-AKAMAI-SECCPT-FLAKE) | 1-3 days | +1 (homedepot) | [01](vNext/01_R-AKAMAI-SECCPT-FLAKE.md) |
| **FIX-D2** webgl/webgl2 context conflation | 2-3 days | infrastructure; partial flips on sites checking both contexts | [07](vNext/07_FIX-D2-D3-WebGL.md) |
| **URL polyfill blob: protocol** | 1-2 days | +0-1 (DataDome iframe boost + duolingo secondary) | [10](vNext/10_URL-polyfill-blob.md) |
| **FIX-G canvas noise A/B + decision** | 2-3 days | +0-3 cross-vendor | [08](vNext/08_FIX-G-canvas-noise.md) |
| **Akamai BMP sensor_data POST trace** (R-BESTBUY-AKAMAI) | 1-2 days trace + 2-5 days fix | +1 (bestbuy) | [03](vNext/03_R-BESTBUY-AKAMAI.md) |
| **R-AWSWAF beyond FIX-J — behavioural enrichment** | 1-2 weeks | +2-3 (final mile on AWS WAF cluster) | [11](vNext/11_R-AWSWAF-FIX-J-deep.md) |

### Track C — `vendor_solvers` work (high cost, decisive lift)

| Idea | Cost | Expected lift | vNext |
|------|------|---------------|-------|
| **DataDome WASM daily-key solver** (private companion crate) | 1-2 weeks | +1-2 (etsy, yelp) | [12](vNext/12_R-DATADOME-WASM.md) |
| **Kasada `/tl` token solver** (private) | months | +3 (canadagoose, hyatt, realtor) but ROI questionable | [06](vNext/06_R-KASADA-FRONTIER.md) |

### What gets us to 115 (back-of-envelope, single-IP)

Starting from ~103/126 (current single-IP-single-trial chrome_148_macos):

**Track A measurement cleanup (no new fixes, just methodology):**
- Per-vendor-aware shuffle (force AWS WAF sites non-adjacent): **+1-3**
- Per-vendor sub-process isolation: **+2-3**
- Inter-site cool-down per vendor cluster: **+1-2**
- Combined Track A lift (overlapping somewhat): **+3-5** → ~106-108 single-trial

Then routed best-of-4 normally adds +5-8 over the best single profile (since each profile flips different sites):
→ **routed best-of-4 ~111-116** with just Track A on the current fix set

**Add Track B public-engine fixes:**
- Per-region accept-language (amazon-de/fr/in/jp get correct locale): **+3**
- FIX-D3 (per-non-macOS GPU fixture alignment): **+1-2 via routed best-of-4**
- Per-Page cookie partition (x-com flips): **+1**
- Akamai sec-cpt primitive (homedepot flips): **+1**

→ **routed best-of-4 ~116-123** which **beats v150's 115**.

**Track C (vendor_solvers) on top adds DataDome + maybe Kasada** but is gated on the private repo's roadmap.

## Recommended next-session sequence (single-IP constrained)

### Sprint 1 — Same-IP measurement clean-up (1-2 days)

The gate currently uses random shuffling but doesn't enforce vendor
spacing. Fix that first so the NEW baseline isn't artificially low.

1. **Per-vendor-aware shuffle** in `benchmarks/run_fix12_gate.sh`: after
   the random shuffle, post-process to ensure no two consecutive sites
   share an antibot vendor (tag each site with its vendor; permute
   adjacents until separated).
2. **Per-vendor sub-process isolation** — modify the gate runner (or
   sweep_metrics) so AWS WAF / DataDome / Akamai cluster sites run in
   FRESH `sweep_metrics` subprocesses. Closes HTTP/2 conn pool + cookie
   jar + V8 isolate state per request, mimicking "fresh visitor".
3. **Inter-vendor-site cool-down** — sleep 30-60 s before the NEXT
   site that hits the same vendor (most sites pay nothing; only
   consecutive AWS-WAF or DataDome sites pay).
4. Re-run the gate. Establish the NEW baseline post-Track-A.

### Sprint 2 — Public-engine lift (1-2 weeks)

5. **Per-region accept-language** (1-2 d) — biggest single lever for
   amazon-de/fr/in/jp/com. amazon-fr expects `fr-FR,fr;q=0.9,en-US;q=0.8`;
   we send `en-US`. Either change per-profile (hairy) or have the HTTP
   layer inject the right `accept-language` based on the URL's TLD
   (cleaner).
6. **FIX-D3** (1-2 d per OS) — `apple_m3_macos` pattern applied to
   `chrome_148_windows` (nvidia) + `chrome_148_linux` (intel). Real
   Chrome captures needed first; see [vNext/07](vNext/07_FIX-D2-D3-WebGL.md).
7. **Per-Page cookie partitioning** (3-7 d) — touches `crates/net/src/lib.rs::SharedSession`.
   Flips x-com; broader production robustness.
8. **Akamai sec-cpt oracle + primitive** (1-3 d) — mirrors awswaf_probe.
   Flips homedepot.

### Sprint 3 — vendor_solvers (out of band)

DataDome WASM + Kasada `/tl` solvers are private-repo work. Coordinate
with the `vendor_solvers` maintainer; not blocking for v0.2.0 public.

### Sprint 4 — Final polish + tag (1 week)

9. Behavioural enrichment per [vNext/11](vNext/11_R-AWSWAF-FIX-J-deep.md).
10. Canvas noise A/B decision per [vNext/08](vNext/08_FIX-G-canvas-noise.md).
11. URL polyfill blob: fix per [vNext/10](vNext/10_URL-polyfill-blob.md).
12. **v0.2.0-rc1 tag** if routed best-of-4 ≥ 115. **v0.2.0-final** if it exceeds 115.

## What didn't work / things to avoid

- **Single-IP serial sweep without vendor spacing is the worst possible measurement environment** for vendor clusters (AWS WAF / DataDome / Akamai). Verified this session: same fixes, same fingerprint, same trial — EU/AU amazon TLDs passed while NA/DE/APAC failed purely because they hit AWS WAF AFTER 3 token issuances in the same vendor cluster. **Always shuffle AND space-out vendor-adjacent sites.** Multi-IP would solve it cleanly but we don't have multi-IP; the next-best thing is per-vendor cool-down + sub-process isolation (Sprint 1 above).
- **Wide profile sampler v1 (5 chip configs varying cores independently)** regressed an 8-site AWS WAF sweep from 1/8 → 0/8 because of cross-API inconsistency (12-core CPU + "Apple M3" GPU = M3 is 8-core base). Tightened to M3-family-only per-chip-coherent in FIX-E2.
- **FIX-D applied only to apple_m3_macos** — other GPU profiles still have the FIX-D-era bugs. The routed best-of-4 median CAN'T benefit from a per-site-best-attempt if 3 of the 4 profiles are broken on the same site. **FIX-D3 is the unlock** (see Sprint 2.6).
- **Treating each cap-truncated gate run as "no data"** — pre-FIX-J-ea9b32d the sweep_metrics example wrote its JSON only on full 126/126 completion. 50-min cap = total data loss. Now fixed with `.partial` checkpointing; cap-truncated runs leave a usable per-site trace.

## Files to read first if you're picking this up cold

1. This doc (`HANDOFF_2026_05_27.md`).
2. [`vNext/00_README.md`](vNext/00_README.md) — the yield × effort map.
3. [`releases/v0.1.0-parity/audit/16_DECISION_LOG.md`](releases/v0.1.0-parity/audit/16_DECISION_LOG.md) — every fix's why + what's next.
4. The specific vNext doc for the task you'll start (each has concrete next-step commands).
5. `benchmarks/run_fix12_gate.sh` — the gate script + methodology.

If you have 30 minutes: read this + `vNext/00_README.md`, then pick the
top item in `vNext/15_FIX_PRIORITY_RANKED.md` your environment unblocks
(multi-IP setup → Track A items; CI access only → Track B items).
